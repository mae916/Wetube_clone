html(lang="ko")
    head
        title #{pageTitle} | Wetube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
        //- favicon.ico 오류로 코드추가
        link(rel="shortcut icon" href="#") 
    body
        header
            h1=pageTitle
            nav
                ul 
                    li 
                        a(href="/videos/upload") Upload Video 
                    li 
                        a(href="/") Home
                    li 
                        a(href="/search") Search
                    li 
                        a(href="/join") Join
                    li 
                        a(href="/login") login
        main
            block content
    include partials/footer