extends base 

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="username" name="username" type="text" required) 
        input(placeholder="Password" name="password" type="password" required) 
        input(type="submit" value="Login")
    hr
    div 
        span 계정이 없으신가요? 
        a(href="/join") Creat one now &rarr;